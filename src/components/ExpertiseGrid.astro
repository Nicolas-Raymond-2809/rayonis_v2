---
import ExpertiseCard from './ExpertiseCard.astro';
import { missions } from '../data/missions';
---

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 p-8 pb-32">
  {missions.map((item) => (
    <ExpertiseCard {...item} />
  ))}
</div>

</div>
